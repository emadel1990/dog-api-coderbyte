import { Box } from '@mui/material';

export const Breed = () => {
	return (
		<Box
			sx={{
				display: 'flex',
				justifyContent: 'center',
				alignItems: 'center',
				width: '100%',
				minHeight: '100vh',
				backgroundColor: '#bdbdbd'
			}}>
			Breed
		</Box>
	);
};
